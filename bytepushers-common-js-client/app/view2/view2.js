'use strict';

/*
require(["app", "bytepushers"], function(myApp, BytePushers) {
  myApp.controller('view2Controller', function ($scope) {
    console.log(Object.isDate(new Date()) + " " + Object.isDate("str"));
  });
});*/
